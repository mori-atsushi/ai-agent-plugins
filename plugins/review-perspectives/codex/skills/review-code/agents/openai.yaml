interface:
  display_name: "Review Code"
  short_description: "Review the current changes from five perspectives"
  default_prompt: "Use $review-code to review the current changes."

policy:
  allow_implicit_invocation: true
